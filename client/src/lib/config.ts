// API Configuration
// In development: Uses relative URLs which get proxied by Vite to localhost:3001
// In production: Uses relative URLs served by the same Express server
export const API_BASE_URL = '/api'